from setuptools import setup

setup(
    name="onnxoptimizer",
    version="0.3.8",
    description="Stub package for onnxoptimizer (aarch64)",
    py_modules=["onnxoptimizer"],
    install_requires=["onnx"],
)
